#!/usr/bin/perl
use feature state;
use hanno_utility;
use dlpoly_utility;
use lammps_utility;
use Math::Trig;
use Math::Vector::Real;
use Cwd;
# use strict;

$i=1;
$j=2;

$test=($i==$j);
if($test) {
  print "$test\n";
}